import { Button } from '@/components/ui/button';

const Header = () => {
   return (
      <header>
         <div>
            <Button variant="destructive" />
         </div>
      </header>
   );
};

export default Header;
